# 桌面与移动浏览器系统测试

测试使用同一组 Playwright 场景分别运行在 1440×900 桌面 Chromium 和 Pixel 7 移动视口。

## 覆盖范围

- 登录、主导航固定顺序、阅读统计一级入口、“更多”无多余展开符号以及横向溢出检查。
- 首页、推荐、收藏、全部书籍和分类页面切换。
- 人为延迟首页概况接口，确认欢迎区和搜索框立即显示，重型分类/热门/推荐请求只在概况结束后启动。
- 推荐原因、推荐反馈以及反馈后的即时刷新。
- 管理后台四个工作区切换，避免所有管理功能堆叠在一个长页面。
- 模拟 3336 本待整理书目，验证首屏只渲染 20 条摘要，选择单本后才加载一个完整编辑表单和对应证据。
- 用户与权限工作区中的书库组、用户组和权限矩阵在桌面及移动视口可达且不产生页面横向溢出。
- 使用测试内生成的最小 PDF 进入阅读器，不依赖真实电子书或本机书库；确认阅读工具和书签/高亮入口可达，顶部工具栏与底部翻页浮条会同步自动隐藏并可重新唤醒。
- 在移动浏览器保存测试 PDF，切断网络并刷新，确认应用外壳、离线书架和 PDF 页面仍可渲染。
- 真实请求 320px WebP 封面缩略图，验证响应格式、尺寸上限和非法尺寸拒绝；桌面与移动列表均使用响应式封面。
- 每个场景保存桌面与移动截图；失败时保留截图、视频和 Playwright trace。

## 执行

```powershell
docker compose up -d
cd web
pnpm exec playwright install chromium
$env:E2E_BASE_URL = "http://127.0.0.1:8085"
$env:E2E_ADMIN_USERNAME = "admin"
$env:E2E_ADMIN_PASSWORD = "你的管理员密码"
pnpm test:e2e
```

测试产物位于 `web/test-results`，HTML 报告位于 `web/playwright-report`，两者均已忽略，不会提交到 Git。

## 当前验证状态

- `pnpm test:e2e:list` 已发现桌面与移动端共 24 个场景，包括首页渐进加载、布局交互、大规模整理队列、站点外壳、离线阅读、响应式封面和真实书籍语料回归。
- 前端类型检查、54 个组件/逻辑测试及生产 Docker 构建已通过。
- 2026-08-02 已在 Pixel 7 移动视口完成导航、首页渐进加载、管理后台、详情页、阅读器和离线 PDF 回归；离线用例实际切断浏览器网络、刷新页面并验证 PDF.js 完成页面渲染。
- 依赖专用回归账号、特定格式语料或仅适用于单一视口的场景会条件跳过；跳过原因会显示在 Playwright 报告中，不记为通过。
- 登录助手会等待“登录表单或已登录导航”稳定出现，避免应用初始化阶段误判导致的并发/时序波动。
- 2026-08-04 补充阅读器界面回归：横向工具栏使用项目绿系滚动条，PDF/EPUB 翻页浮条跟随阅读工具自动隐藏；阅读统计提升为一级导航，“更多”不再显示三角符号。桌面与移动布局交互套件 10 项通过，2 项推荐反馈因当前测试库没有候选数据按条件跳过，无失败。
- 2026-08-07 补充导入历史与手机导航回归：导入记录列表改为不可收缩的纵向滚动列表，808px 视口下 6 张卡片均保持 97.5px 高且内容完整，列表高度 500px、滚动内容高度 630px；390×844 手机视口下“更多”弹窗以完整导航区域定位，左右边界均在视口内，无横向溢出，菜单和账号三角符号均隐藏。项目最小 320px 内容宽度复测无横向溢出。
